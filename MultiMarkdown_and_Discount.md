# <%= @title %>

By default, Marked renders using MultiMarkdown. This supports all standard Markdown, plus tables, footnotes, fenced code blocks, metadata and other extensions.

Alternatively, you can switch to the Discount processor in the {% prefspane Processor %}. This is a GitHub Flavored Markdown version of the Discount processor and should provide syntax parity with GitHub's own parser.

See {% appmenu Help, Markdown Reference %} for more details.
