# <%= @title %>

You can use the gear menu->Proofing submenu to start checking spelling {% kbd cmd ; %}, and opt to also check for grammar suggestions {% kbd shift cmd ; %}.

To navigate errors, use {% kbd opt cmd ; %} (next) and {% kbd opt shift cmd ; %} (previous).

Click the marker on the left to see details about the error, including suggestions for fixing.
