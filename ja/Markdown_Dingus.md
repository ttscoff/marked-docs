<!-- MT-DRAFT: machine translation; human review required -->

# <%= @title %>

Markdown Dingus は、次のような用途に特化したテスト ツールです。
さまざまな Markdown プロセッサがどのように処理するかを理解できました。
内容。 3 ペインのインターフェイスを提供し、
Markdown を編集し、生成された HTML ソースを表示し、
プレビューを同時にレンダリングします。

ディンガスはマークズといくつかの低レベルの処理を共有します。
プレビュー (フェンスで囲まれたコード ブロックの特別な処理など)。
[カスタム ルール]は__実行されません__(Custom_Processor.html)
（指揮者）。ほとんどのデフォルト設定が使用され、設定は無視されます
「GitHub 改行」や「GitHub チェックボックス」など、
結果は通常のマーク付きの結果とは異なる場合があります。
プレビュー。

## カスタム ルールは適用されません [custom-rules-do-not-apply]

Dingus は __プロセッサ サンドボックス__ です。Markdown は次のようになります。
選択した内蔵プロセッサ (MultiMarkdown、
CommonMark (GFM)、Discount、または Kramdown)。 【カスタムルール】(Custom_Processor.html)
そこでは決して実行しないでください。前処理アクションや検索/置換ルールはありません。
シェル コマンド、テキスト/ファイルの挿入、または他の Conductor ステップ。

通常のプレビューでは、カスタム ルールによってマークダウンが変更される可能性があります
レンダリングの前に、スタイルを設定し、CSS または JavaScript を挿入し、
もっと。 Dingus で編集する場合、そのようなことは起こりません。
Dingus が開いている間にカスタム ルールを変更しても、
プレビューを更新します。

Dingus __can__ は同じ [プレビュー CSS スタイル](Custom_Styles.html)
メイン ウィンドウとして (出力ペインのスタイル メニュー経由)。
それは見た目だけです。 Custom を実行するのと同じではありません
ルール。

__Dingus で開く__ プレビューからドキュメントをロードします
現在のマークダウンバッファ。カスタム ルールがすでに実行されている場合
そのファイルはマーク付きで開かれているため、その効果は次の場所で確認できます。
テキスト (ルールによって挿入されたテキストなど) ですが、
Dingus は、入力時にルールを再適用しません。カスタムをテストするには
ルール、標準のマーク付きプレビューを使用するか、Dingus から保存します
Marked__ の __Open を使用してファイルを開きます。

## 「ディンガス」とは何ですか? [what-is-a-dingus]

「ディンガス」とは、Web 開発から借用された用語です。
単純なテスト ツールまたはサンドボックス環境を指します。の
Markdown Dingus を使用すると、Markdown 構文を試したり、
さまざまなプロセッサがそれをどのように解釈するかをすぐに確認できます。

## ディンガスへのアクセス [accessing-the-dingus]

Markdown Dingus には次からアクセスできます。
[{% appmenu Help, Open Markdown Dingus %}][2]。それは特に
次の場合に役立ちます:

* 新しい Markdown 構文の学習
* レンダリングの問題のトラブルシューティング
* 異なるプロセッサーからの選択
* 複数にまたがって機能する必要があるドキュメントの作成
  システム

## 3 ペインのレイアウト [three-pane-layout]

![][1]

Dingus ウィンドウは、次の 3 つの同期されたペインに分割されています。

### 1. マークダウン入力 (左ペイン) [1-markdown-input-left-pane]

* __構文の強調表示__: マークダウンは次のように強調表示されます。
  構造を明確にするための色
* __ライブ編集__: 入力すると変更がすぐに反映されるのを確認します
  他のペインでは
* __大きいフォント__: 快適な編集のための 21pt Menlo フォント

__オプション ドロップダウン__ (左ペインの右上):
**オプション** メニューで次を切り替えることができます。

* __行番号を表示__: 左側に余白を表示します。
  段落ごとに 1 つの行番号 (折り返された行は 1 行としてカウントされます)
  行）。
* __非表示部分を表示__: スペースを中点 (・) として表示し、タブを次のように表示します。
  右向き矢印 (→)、およびキャリッジリターンとしての改行
  記号 (↵) が明るい灰色で表示され、迷走している箇所を見つけることができます。
  空白。
* __グレムリンのハイライト__: 背景を明るい赤にします
  非 ASCII 文字 (波引用符、絵文字など) と暗い文字
  問題のある非表示文字の背景が赤い (例:
  幅ゼロのスペース）。通常のタブ文字と改行文字は
  強調表示されていません。

選択内容は保存され、次回開いたときに復元されます
ディンガス。

__検索バー__: **⌘F** を押すと、検索バーが下に表示されます。
「マークダウン入力」ラベル。で検索および置換できます。
エディターでは、「次を検索」には **⌘G** を使用し、「検索」には **⇧⌘G** を使用します
前に移動し、1 つまたはすべての一致を置き換えます。閉じるを押します
ボタンまたは **⌘F** を再度押すと、検索バーが非表示になります。

### 2. HTML ソース (中央ペイン) [2-html-source-middle-pane]

* __生成された HTML__: 選択された HTML を正確に確認します
  プロセッサが作成する
* __構文の強調表示__: HTML はわかりやすく色分けされています。
  読書

### 3. レンダリングされたプレビュー (右ペイン) [3-rendered-preview-right-pane]

* __ライブ プレビュー__: マークダウンがどのように表示されるかを確認します。
  レンダリングされた
* __絵文字サポート__: `:zzz:` のような GitHub スタイルの絵文字は、
  画像に変換された
* __自動スクロール__: 自動的にスクロールして、
  現在の編集位置

## Dingus での編集 [editing-in-the-dingus]

Markdown 入力ペインには、スマートな編集機能が含まれています。
Markdown の記述をより速く、より自然にします。

### スマート改行 (リターンキー) [smart-newline-return-key]

Return キーを押すと現在の行に適応します。

* __Lists__: リスト行 (`-`、`*`、`+`、または `1.`)、
  正しいマーカーを持つ新しいリスト項目を挿入します。番号付き
  リストは次の番号に続きます。
* __Blockquotes__: `>` で始まる行に、
  新しいブロック引用行。
* __コードフェンス__: 3 つ以上のバッククォートがある行上
  (例: ` ``` `)、開始部分の間に空行を挿入します。
  そして柵を閉める。
* __その他の行__: 通常の改行を挿入します。

### キャラクターの組み合わせ [character-pairing]

開始文字を入力すると、エディタは自動的に
終了文字を挿入し、カーソルを間に置きます。
彼ら。サポートされているペア: `"` `'` `(` `[` `` ` `` `<` 。

* __選択あり__: 選択したテキストをペアで囲みます。
* __選択なし__: カーソルのあるペアを挿入します
  彼らの間で。
* __Type-over__: 次の文字がすでに
  終了区切り文字を再度入力すると、カーソルが過去に移動します
  複製を挿入する代わりにそれを使用します。
- __空のペア内のスペース__: カーソルが空のペア (例: `(|)`) の間にある場合、スペースを入力すると終了文字がスペースに置き換えられます。

### ショートカット キー [shortcut-keys]

|ショートカット |アクション |
| :----------- | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **⌘F** | Markdown 入力ペインの検索バーを表示または非表示にする |
| **⌘G** |次を検索 (検索バーが表示されている場合) |
| **⇧⌘G** |前を検索 (検索バーが表示されている場合) |
| **⌘B** |太字: 選択範囲を `**` で折り返すか、| の間にカーソルを置いて `**\|**` を挿入します。
| **⌘私** |斜体: 選択範囲を `_` で折り返すか、| の間にカーソルを置いて `_\|_` を挿入します。
| **⇧⌘L** |サイクル リスト マーカー (順序なし ↔ 順序あり) |
| **タブ** |行またはリスト項目をインデントする |
| **Shift+Tab** |行またはリスト項目のインデントを解除する |
| **⌃⌘→** |行またはリスト項目のインデント (タブと同じ) |
| **⌃⌘←** |行またはリスト項目のインデントを解除します (Shift+Tab と同じ) |
| **⌃⌘↑** |段落を上に移動 (改行を含む段落を切り取り、1 行上に貼り付け) |
| **⌃⌘↓** |段落を下に移動 (改行を含む段落を切り取り、1 行下に貼り付けます) |
| **⌘K** | Markdown リンクを挿入またはラップします。選択範囲を `[text]()` としてラップし、カーソルを URL に配置します。選択範囲がない場合は、カーソルを括弧の間に入れて `[]()` を挿入します。
| **F6** |マジック参照リンク: 選択範囲を `[text][n]` としてラップし、文書の最後に `[n]: ` 定義を追加します。キャレットが既存の参照上にある場合、インライン マーカーとその定義の間をジャンプします。
| **F7** |マジック脚注: キャレット (または現在の単語の後ろ) に `[^n]` を挿入し、文書の末尾に一致する `[^n]: ` 定義を追加します。キャレットが既存の脚注上にある場合、マーカーとその定義の間をジャンプします。
| **⌘U** |出力ペインを切り替えます (ソース/プレビュー) |
| **⌥⌘↑** |選択範囲の展開: 単語 → 内部/外部区切り文字 → 文 → 段落 → 連続ブロック (表やコード ブロックなど) → 囲むリスト/ブロック引用符/コード ブロック → ドキュメント |
| **⌥⌘↓** |選択範囲を同じレベルまで元のキャレット位置まで縮小します。

Tab/Shift+Tab (および ⌃⌘←/⌃⌘→) はリスト構造を尊重し、
blockquotes: リスト項目のインデント/インデント解除、または追加または
ブロック引用行から`>`を削除します。段落を上下に移動
カーソルの下にある段落全体 (その段落を含む) を選択します。
末尾の改行) を切り取って、上または下に貼り付けます。
隣接する段落が結合しないようにします。

### マジックリンクと脚注 (F6 / F7) [magic-links-and-footnotes-f6-f7]

Dingus エディタは __reference-style リンク__ を構築できます。
__脚注__、次に利用可能な番号を割り当てます
そして、一致する定義を末尾に追加します。
文書。

* __F6 (マジック リファレンス リンク)__: テキストが選択されている状態で折り返す
  選択範囲を `[text][n]` として、新しい `[n]: ` 行を追加します
  文書の最後にあるので、URL を入力できます。の作成
  新しい参照リンクには選択が必要です。キャレットが
  すでに参照リンクまたはその定義上にあります、**F6**
  インラインマーカーと定義の間をジャンプします（または
  定義が存在しない場合は作成されます)。
* __F7 (マジック脚注)__: 番号付き脚注マーカーを挿入します。
  `[^n]` キャレット --- または現在の単語の後
  キャレットは 1 の中にあり、`[^n]: ` に
  脚注本文がある場合は、選択したテキストを脚注本文として使用します。
  選択。キャレットが既存の脚注マーカー上にある場合
  または定義では、**F7** でそれらの間をジャンプします。

参照番号と脚注番号は自動的に選択されるため、
ID を手動で追跡する必要はありません。どちらのショートカットも実行されない
囲われたコードブロックまたはインデントされたコードブロックの内側。

### スマート URL ペースト [smart-url-paste]

貼り付けたときにクリップボードにフォームの URL が含まれている場合
`protocol://...` スペースなし:

* __選択範囲あり__: 選択範囲は
  マークダウンリンク: `[selected text](url)` 。
* __選択なし__: URL は
  自己リンク: `<url>` 。

これにより、コピーした URL を簡単にリンクに変換できます。
手動入力。

### スマートな選択 (⌥⌘↑ / ⌥⌘↓) [smart-selection]

Dingus エディタは __セマンティック選択拡張__ をサポートしています。

* __⌥⌘↑__ はキャレットから始まり、選択範囲を展開します
  経由:
	- 現在の単語
	- 内側と外側の区切りコンテンツ (引用符、括弧、
   括弧、および囲われたコードブロック)
	- 現在の文
	- 現在の段落
	- 連続した空白ではない行のブロック (たとえば、
   テーブル全体、または空白行のない複数行のコード ブロック)
	- 囲んでいるリスト項目、ブロック引用符、またはコードブロック
	- 文書全体
* __⌥⌘↓__ は同じレベルを下って、最後まで戻ります。
  元のキャレット位置に戻ります。

各プレスは常に厳密に大きいか小さい方向に移動します。
を選択するため、その間に「no-op」キーが押されることはありません。
拡大または縮小します。

## Dingus をエディタとして使用する [using-the-dingus-as-an-editor]

ディンガスはフル機能のテキストに代わるものではありません
エディターですが、__クイック編集には非常に便利です。
ライブ プレビュー__、特にどのように表示されるかを正確に確認したい場合
変更がレンダリングされます。テキスト編集動作のすべて
[Dingus での編集][3] で説明されている内容がここに適用されます。

### ファイルを開く/新しいファイルを作成する [opening-a-filecreating-a-new-file]

* __Dingus で新しいファイルを作成__
	- __{% appmenu File, New, New Markdown File %}__ を選択してください
   (⌘N)。
	- プロンプトが表示されたら、__Dingus__ を選択して、新しい未保存のファイルを開始します。
   ディンガスの文書。
	- サンプル コンテンツが選択された状態で新しい Dingus ドキュメントが開きます。
   入力を開始するだけで置き換えられます。
* __Dingus で既存のファイルを開きます__
	- __{% appmenu File, Open in Dingus... %}__ (⌥⌘O) を使用するか、
   Dingus ウィンドウがアクティブな状態で、ステータスの __Open...__ をクリックします。
   バー。このコマンドは、必要に応じて Dingus ウィンドウを開きます。
   「開く」パネルを示します。
	- サポートされているプレーンテキスト/マークダウン ファイルを選択します。その
   コンテンツは現在の Dingus バッファを置き換えます。
* __Dingus でマークされたプレビュー ドキュメントを開く__
	- 通常のプレビュー ウィンドウから、__{% appmenu Preview,
   Open in Dingus %}__ (⌥⌘E) を使用します。
	- 現在のドキュメントのマークダウンが Dingus にロードされます
   元のファイルに手を触れずに実験できるようにする
   保存を選択するまで。カスタムルールは適用されません
   ディンガス。 [カスタム ルールは適用されません](#custom-rules-do-not-apply) を参照してください。

### ファイルを保存する [saving-a-file]

* __現在のファイルに変更を保存__
	- Dingus ウィンドウで、ステータス バーの __Save__ をクリックします。
   または使用します
    __{% appmenu File, Save Dingus %}__ (⌘S)。
	- Dingus ドキュメントがまだ保存されていない場合は、
   場所とファイル名の入力を求められます。その後、⌘S
   同じファイルを更新します。
* __名前を付けて保存/新しいファイルに複製__
	- __{% appmenu File, Save Dingus As... %}__ (⌥⌘S) を使用します。
	- これにより、常に __Save As__ ダイアログが開き、
   現在の Dingus のコンテンツを上書きせずに新しいファイルにコピーします
   オリジナル。

### マーク付きでのプレビュー [previewing-in-marked]

* __Dingus ドキュメントを完全なマーク付きプレビューとして開きます__
	- Dingus ステータス バーの [__マークで開く__] をクリックするか、次のコマンドを使用します。

    __{% appmenu File, Open Dingus in Marked %}__ (⌘P)。
	- Dingus ドキュメントが保存されていない場合、または保存されていない変更がある場合、
   最初に保存するように求められます。その後、マークが開きます
   そのファイルの標準プレビュー。

これらのコマンドを使用すると、ディンガスを
簡単な修正と実験のための軽量エディター、その後
完全なマーク付きプレビューまたは通常のエディターにジャンプします。
より広範な編集を行う準備が整いました。

## プロセッサの選択 [processor-selection]

上部のドロップダウンを使用して、異なるものに切り替えることができます。
マークダウンプロセッサ:

* __MultiMarkdown__: テーブルを備えたフル機能のプロセッサ、
  脚注と数学のサポート
* __CommonMark (GFM)__: に準拠した標準の高速プロセッサ
  CommonMarkの仕様
* __割引__: タスク付きの GitHub フレーバー マークダウン
  リストとテーブル
* __Kramdown__: 追加機能を備えた高度なプロセッサー
  IALやタイポグラフィーなど

## ディンガスを使用する理由 [why-use-the-dingus]

### プロセッサーの違いを理解する [understanding-processor-differences]

Markdown プロセッサが異なれば、構文の処理方法も異なります。の
ディンガスは次のことに役立ちます。

* __出力の比較__: 各プロセッサーがどのようにレンダリングするかを正確に確認します
  同じマークダウン
* __デバッグの問題__: 特定の構文が機能しない理由を特定する
  期待どおりに動作する
* __構文を学ぶ__: 微妙な違いを理解する
  プロセッサ実装間

### 書く前のテスト [testing-before-writing]

特定の Markdown スタイルにコミットする前に、
書類:

* __Validate Syntax__: マークダウンがレンダリングされることを確認します。
  正しく
* __プロセッサの選択__: どのプロセッサが最適に動作するかを決定します
  あなたのコンテンツのために
* __安全に実験__: 影響を与えずに新しい構文を試します
  実際の書類

## 一般的な使用例 [common-use-cases]

### テーブル構文の違い [table-syntax-differences]

プロセッサによっては、テーブル構文の処理方法が異なる場合があります。ディンガス
どのプロセッサがテーブルを最適にサポートしているかを確認できます
書式設定が必要です。

### 脚注のサポート [footnote-support]

すべてのプロセッサが脚注をサポートしているわけではありません。ディンガスを使用して、
脚注構文が選択したプロセッサで動作することを確認してください。

### 数学と特殊文字 [math-and-special-characters]

さまざまなプロセッサが数学をどのように処理するかをテストする
式と特殊なタイポグラフィ文字。

## 効果的に使用するためのヒント [tips-for-effective-use]

1. __Start Simple__: 基本的な Markdown から始めて、徐々に始めていきます。
   複雑さを加える
2. __エッジケースのテスト__: 珍しい構文の組み合わせを試してください。
   プロセッサーの制限を理解する
3. __並べて比較__: プロセッサーを切り替えて、
   違いがはっきりわかる
4. __実際のコンテンツを使用__: 実際のコンテンツを
   現実世界のシナリオをテストするためのドキュメント

Dingus は、次のことをしたい人にとって強力なツールです。
さまざまな Markdown 実装のニュアンスを理解する
コンテンツがさまざまな環境で正しくレンダリングされることを確認します
プラットフォームとプロセッサー。

[1]: images/dingus-interface-800.jpg @2x width=800px height=573px class=center
[2]: x-marked-3://dingus
[3]: #editing-in-the-dingus