<!-- MT-DRAFT: machine translation; human review required -->

# <%= @title %>

スタイル マネージャーは、すべてのスタイルを管理するための集中インターフェイスを提供します。
組み込みスタイルとカスタム スタイル。どれを選択するかを完全に制御できます。
スタイルは、メニュー、その順序、キーボード ショートカットなどに表示されます。

## スタイルマネージャーを開く [opening-the-style-manager]

スタイル マネージャーを開くには、{% prefspane Style %} の [**スタイルの管理…**] ボタンをクリックします。
ペインを選択するか、{% appmenu Style, Manage Styles (~@$m) %} を使用します。 CSS ファイルを環境設定ウィンドウに直接ドラッグすることもできます --- マーク付き
それらをインポートし、スタイル マネージャーを開き、新しく追加された行を選択します。
あなた。

![スタイルマネージャー][img-style-manager]

  [img-style-manager]:images/screenshots/style-manager.jpg @2x width=1009px height=517px class=center

## スタイルテーブル [the-style-table]

スタイル マネージャーは、すべてのスタイルを並べ替え可能なテーブルに表示します。
組み込みスタイルとカスタム スタイルをシームレスに実現します。テーブルの各行にはいくつかの情報が含まれます。
列:

### 有効チェックボックス [enabled-checkbox]

**有効** チェックボックスをオンにすると、すぐにスタイルがスタイルに追加または削除されます。
メニュー、デフォルト スタイル ポップアップ、およびキーボード ショートカット。スタイルを無効にすると、
メニューには表示されませんが、スタイル マネージャーには残っているため、簡単に再有効化できます。

現在アクティブなスタイルを無効にすると、Marked は自動的に
次に利用可能な有効なスタイル。

### 名前列 [name-column]

**名前** 列には、スタイルの表示名が表示されます。この名前は編集できます
直接クリックしてインライン化します。変更は保持され、すべてのメニューに反映されます
スタイルが現れる場所。これは、カスタム スタイルで特に便利です。
ファイル名よりもわかりやすい名前が必要な場合があります。

組み込みスタイルの名前はロックされており、編集できません。カスタマイズするには
組み込みスタイルの名前を複製して、編集可能なコピーを作成します。

### ソース列 [source-column]

**ソース** 列は、スタイルの出所を示します。

- **組み込み**: Marked に付属し、アプリケーションに保存されるスタイル
  バンドル
- **カスタム**: ドライブ上の CSS ファイルから追加したスタイル
- **複製**: 別のスタイル (組み込みのスタイル) を複製して作成されたスタイル
  またはカスタム)

### アクション列 [actions-column]

各行には、そのスタイルを管理するためのボタンを備えた **アクション** スタックが含まれています。

**編集**: デフォルトのエディタで CSS ファイルを開きます。組み込みスタイルは使用できません。
直接編集 -- 編集可能なコピーを作成するには、まずそれらを複製する必要があります。

**複製**: スタイルのコピーと新しい CSS ファイルをディスク上に作成します。の
重複したスタイルは、テーブル内の元のスタイルのすぐ下に表示されます。これは
組み込みスタイルをカスタマイズする推奨方法。

**表示**: Finder に CSS ファイルを表示し、ファイルを見つけやすくします。
あなたのドライブ。このボタンは、ファイル パスを含むカスタム スタイルでのみ使用できます。

**削除**: マークからスタイルを削除します。カスタム スタイルの場合は、次のようになります。
参照のみを削除する (CSS ファイルを保持する) か、移動するかのオプション
CSS ファイルをゴミ箱に移動します。組み込みスタイルは削除できませんが、削除することはできます。
無効になっています。

**復元**: 組み込みスタイルの場合、このボタンはスタイルを元の状態に復元します。
変更されている場合はデフォルトの状態。このボタンは次の場合にのみ表示されます
組み込みのスタイル。

## スタイルの並べ替え [reordering-styles]

行はドラッグ アンド ドロップで並べ替えることができます。スタイル行を新しい行にドラッグするだけです
テーブル内の位置。ここで設定した順序によって次のことが決まります。

- Marked のメニューのスタイル メニューの順序
- キーボード ショートカットの割り当て (最初の 9 つの有効なスタイルの場合は `⌘1`–`⌘9`、
  次の 10 については `⌥⌘1`–`⌥⌘0` など)

スタイルを希望のキーボード ショートカット スロットにドラッグします。
占領する。

## スタイルの追加 [adding-styles]

新しいカスタム スタイルをスタイル マネージャーに追加するには、いくつかの方法があります。

### 追加ボタン [add-button]

[**新しいスタイルの追加**] ボタンをクリックしてファイル ピッカーを開きます
ここで、インポートする 1 つ以上の CSS ファイルを選択できます。選択したファイルは、
スタイル マネージャーに追加され、デフォルトで有効になります。

### ドラッグアンドドロップ [drag-and-drop]

CSS ファイルをスタイル マネージャー ウィンドウに直接ドラッグできます。ドラッグすると
ファイルをウィンドウ上に置くと、「カスタム スタイルの追加」を示すオーバーレイが表示されます (または
複数のファイルの場合は「N 個のカスタム スタイルを追加」)。ファイルをドロップしてインポートします。

CSS ファイルをテーブル内の特定の位置にドラッグすることもできます (ドロップ)。
インジケーターは新しいスタイルが挿入される場所を示し、制御できるようにします。
インポートと配置の両方を 1 つのアクションで実行できます。

CSS ファイルを {% prefspane Style %} 設定ペインにドラッグすると、
それらをインポートすると、スタイル マネージャーが自動的に開きます。

## ライブプレビュー [live-preview]

スタイル マネージャーの右側のペインには、選択したスタイルのライブ プレビューが表示されます。
スタイル。プレビューでは、見出し付きの包括的なサンプル ドキュメントが表示されます。
リスト、テーブル、コード ブロック、ブロック引用符、その他の一般的な Markdown 要素、
すべて、選択したスタイルの実際の CSS でスタイル設定されます。

プレビューではディスクから直接 CSS ファイルを使用するため、
外部エディターはプレビューで即座に更新されます。これにより、
カスタム スタイルを開発しながら、変更をリアルタイムで確認できます。

### ダークモードのプレビュー [dark-mode-preview]

プレビューの上にあるチェックボックスを使用すると、ライト モードとダーク モードを切り替えることができます。
プレビュー。これは、両方の外観モードでスタイルがどのように見えるかをテストするのに役立ちます。
特にシステムの外観に適応するスタイルを作成している場合はそうです。

## キーボード ショートカット [keyboard-shortcuts]

スタイル マネージャーは、キーボードの操作方法を示す凡例を表の下に表示します。
ショートカットが割り当てられます。最初の 9 つの有効なスタイルは {% kbd cmd 1 %} から
{% kbd cmd 9 %} ({% kbd cmd 0 %} は予約されています)、次の 10 人は {% kbd opt cmd 1 %} から {% kbd opt cmd 0 %} を受け取ります。割り当てられたキーボード ショートカットは、プレビューのスタイル ポップアップ メニューで確認できます。

## 無効なスタイルのフィルタリング [filtering-disabled-styles]

ウィンドウの下部にあるチェックボックスを使用して、無効になっているものを表示または非表示にすることができます
スタイル。チェックを外すと、有効なスタイルのみが表示されるため、
アクティブなスタイルに焦点を当てて並べ替えます。チェックすると、すべてのスタイル (有効および無効)
が表示され、完全なスタイル コレクションを管理できます。

## 組み込みスタイルの復元 [restoring-builtin-styles]

ウィンドウの下部にある **すべての組み込みスタイルを復元** ボタン
すべての組み込みスタイルをデフォルトの状態に戻します。これは、次の場合に便利です。
組み込みスタイルを無効にし、再度有効にしたい場合、またはリセットしたい場合
組み込みスタイルに加えられた変更。

## ヒント [tips]

- **頻度別に整理**: 最もよく使用するスタイルを一番上にドラッグして、
  最も簡単なキーボード ショートカット ({% kbd cmd 1 %}、{% kbd cmd 2 %} など)

- **削除ではなく無効化**: 使用していないスタイルを削除するのではなく、
  それらを無効にします。邪魔にならないだけで、必要に応じて利用できます
  それらは後で。

- **実験のために複製を使用**: メジャーを作成する前にスタイルを複製します。
  いつでも元の状態に戻れるように変更します。

- **編集中のプレビュー**: CSS の編集中はスタイル マネージャーを開いたままにしてください
  ファイルを参照して、プレビュー ペインで変更がリアルタイムで更新されることを確認します。

- **バッチインポート**: を使用すると、複数の CSS ファイルを一度に選択できます。
  [追加] ボタンを使用するか、複数のファイルをドラッグして 1 回の操作ですべてをインポートします。