# Marked Localization Update
# Locale: ja (Japanese)
# Feature: convertPlusPageBreaks (Treat +++ as page breaks)
# Generated: 2026-08-03
#
# HOW TO TRANSLATE
# ----------------
# Fill in or correct the string on each line: "key" = "your translation";
# Keep keys unchanged. Do not translate product names (Marked, iA Writer)
# or the literals +++ and <!--BREAK--> unless your locale normally does.
#
# Return this file with translations filled in (same filename).
# We merge with: python3 scripts/merge_localization_updates.py <this-file>

=== BEGIN Settings.strings ===
"setting.convertPlusPageBreaks" = "+++ を改ページとして扱う";
# EN: Treat +++ as page breaks
# DRAFT: +++ を改ページとして扱う

"tooltip.convertPlusPageBreaks" = "有効にすると、3 つ以上の + 文字の行が改ページとして扱われます (<!--BREAK--> と同じ)。 iA Writer と互換性があります。デフォルトではオフです。";
# EN: When enabled, a line of three or more + characters is treated as a page break (same as <!--BREAK-->). Compatible with iA Writer. Off by default.
# DRAFT: 有効にすると、3 つ以上の + 文字の行が改ページとして扱われます (<!--BREAK--> と同じ)。 iA Writer と互換性があります。デフォルトではオフです。
=== END Settings.strings ===
